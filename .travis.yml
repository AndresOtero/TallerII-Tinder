language: cpp
sudo: required
dist: vivid
compiler: gcc
os: linux
ruby: 2.2.0

before_script: ./beforeinstall.sh
install: true
script: ./build.sh

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5.1
    - g++-5.1
    - clang
